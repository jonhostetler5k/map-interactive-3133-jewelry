// Redirecting to the modular content structure to avoid compilation errors with large strings
import { FULL_MARKETING_PLAN } from './src/content/index';

export const MARKETING_PLAN_CONTENT = FULL_MARKETING_PLAN;
